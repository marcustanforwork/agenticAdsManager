# `@ads/db`

**Job:** Drizzle schema, migrations and typed repositories, including the proposal state machine (BLUEPRINT §4, §3.9). Built in M01a.

**May depend on:** `@ads/contracts`.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
