# `@ads/connector-google-write`

**Job:** The Google Ads WRITE client and Data Manager conversion uploads (implements `PlatformWriteClient`). **Only `@ads/gateway` may depend on this package.** Built in M13.

**May depend on:** `@ads/contracts`, `@ads/connector-google`; `@ads/connector-testing` as a devDependency.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
