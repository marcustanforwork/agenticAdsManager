# `@ads/connector-meta-write`

**Job:** The Meta WRITE client and Conversions API (implements `PlatformWriteClient`). **Only `@ads/gateway` may depend on this package.** Built in M12.

**May depend on:** `@ads/contracts`, `@ads/connector-meta`; `@ads/connector-testing` as a devDependency.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
