# `@ads/pack-sdk`

**Job:** `definePack`, the pack registry, the threshold and copy-check engines, and the manifest publisher. Built in M05a.

**May depend on:** `@ads/contracts`.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
