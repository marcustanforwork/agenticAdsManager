# `@ads/evals`

**Job:** Replay cases, scoring and model comparison. Built in M08 (v0) and M16a.

**May depend on:** `@ads/contracts`, `@ads/db`, `@ads/core`, `@ads/pack-sdk`, and the packs.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
