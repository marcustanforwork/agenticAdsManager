# `@ads/vault`

**Job:** Envelope encryption for platform tokens, with separate read and write master keys (PROPOSAL §6.13). Built in M01b.

**May depend on:** `@ads/contracts`, `@ads/db`.

The rules are BLUEPRINT §2, enforced by `pnpm check:boundaries` (see `scripts/boundary-rules.mjs`).
